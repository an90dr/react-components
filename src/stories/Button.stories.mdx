import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import pkg from '../../package.json';
import Button from '../components/Button';
import Space from '../components/Space';
import Loading from '../components/Loading';

<Meta
  title={'React Components Free v.' + pkg.version + '/Button'}
  component={Button}
  argTypes={{
    HTMLProps: { control: { type: 'object' } },
    buttontype: {
      control: { type: 'radio' },
      options: ['primary', 'secondary', 'link'],
    },
    size: {
      control: { type: 'radio' },
      options: ['small', 'medium', 'large'],
    },
  }}
/>

export const Template = (args) => (
  // buttontype={'primary'} label={'Primary Button'} {...args}
  <Button buttontype={'primary'}>Button</Button>
);

# Button

### Button primary

export const Primary = Template.bind({});
Primary.args = {
  buttontype: 'primary',
};

<Story name='Primary'>{Primary}</Story>

```jsx
<Button buttontype='primary'>Primary Button</Button>
```

### Button secondary

<Story name='Secondary'>
  <Button buttontype='secondary'>Secondary Button</Button>
</Story>

```jsx
<Button buttontype='secondary'>Secondary Button</Button>
```

### Button link

<Story name='Link'>
  <Button buttontype='link'>Link Button</Button>
</Story>

```jsx
<Button buttontype='link'>Link Button</Button>
```

### Button Disabled

export const ButtonDisabled = Template.bind({});
ButtonDisabled.args = {
  label: 'Button Disabled',
  disabled: true,
};

export const LinkDisabled = Template.bind({});
LinkDisabled.args = {
  label: 'Link Disabled',
  buttontype: 'link',
  disabled: true,
};

<Story name='Disabled'>
  <Button HTMLProps={{ disabled: 'disabled' }}>Button Disabled</Button>
</Story>

<Story name='Link Disabled'>
  <Button buttontype='link' HTMLProps={{ disabled: 'disabled' }}>
    Link Disabled
  </Button>
</Story>

```jsx
<Button HTMLProps={{disabled:'disabled'}}>Button Disabled</Button>
<Button buttontype='link' HTMLProps={{disabled:'disabled'}}>Link Disabled</Button>
```

### Button on Click

<Story name='onClick Event'>
  <Button
    HTMLProps={{
      onClick: (event) => {
        console.log('Button Clicked');
      },
    }}
  >
    Button Click
  </Button>
  <p>Check console</p>
</Story>

<!--export const ButtonOnClick = Template.bind({});
ButtonOnClick.args = {
  label: 'Button on click',
};
ButtonOnClick.argTypes = {
  HTMLProps: {onClick: { action: 'clicked' }},
};

<Story name='onClick Event1'>{ButtonOnClick}</Story>
-->

```jsx
<Button
  label="Secondary Button"
  HTMLProps={
    onClick={(event: any) => {
      console.log(event);
    }}
  }
></Button>
```

### Button Size

<Story name='small/medium/large'>
  <Button size={'small'}>Button Small</Button>
  <Space size={10} />
  <Button size={'medium'}>Button Medium</Button>
  <Space size={10} />
  <Button size={'large'}>Button Large</Button>
</Story>

```jsx
<Button
  size={'small'}
>Button Small</Button>

<Button
  size={'medium'}
>Button Medium</Button>


<Button
  size={'large'}
>Button Large</Button>
```

### Button with Loading

<Story name='with Loading'>
  <Button size={'large'}>
    <Loading size={'xsmall'} />
    Button Small
  </Button>
</Story>

# Button Component API

<ArgsTable of={Button} />
